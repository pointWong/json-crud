{
  "name": "city-json",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scss":"sass --watch style/scss/index.scss --no-source-map --style compressed style/css/index.css",
    "start":"node --watch -- ./index.js",
    "dev": "pnpm run scss & pnpm run start",
    "dev:windows":"run-p scss start"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.77.1"
  }
}
